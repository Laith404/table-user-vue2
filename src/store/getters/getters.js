export default{
    PEOPLE: state => {
        return state.people;
      },
      SEARCH_VALUE: state => {
        return state.searchValue
      },
      PARAM_SORT: state => {
        return state.paramSort
      }
}